import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.finbox.riskioniccapacitorsample',
  appName: 'RiskIonicCapacitorSample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
